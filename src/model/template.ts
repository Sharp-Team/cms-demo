const productDetailJsonTpl = `{
  "productName": {
    "type": "input"
  },
  "description": {
    "type": "html"
  }
}`;

const productDetailModel = JSON.parse(productDetailJsonTpl);

export { productDetailModel };
